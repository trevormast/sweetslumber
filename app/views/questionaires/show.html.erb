<p id="notice"><%= notice %></p>

<div class="col-lg-6">
  <table class="table table-bordered">
    <thead>
      <th></th>
      <th></th>
    </thead>
      <% @questionaire.attributes.each do |key, value| %>
        <tr>
          <td><strong><%= key %>:</strong></td>
          <td><%= value %></td>
        </tr>
      <% end %>
  </table>
</div>

<%= link_to 'Back', workshops_path %>
