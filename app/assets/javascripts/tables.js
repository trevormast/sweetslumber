$(document).ready(function() {
    var table = $('.datatable').DataTable( {
        responsive: true,
        bPaginate: false,
        bFilter: false
    } );

    // new $.fn.dataTable.FixedHeader( table );
} );
